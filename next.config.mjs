/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "api.smk.dk",
      "another-api.com",
      "images.unsplash.com",
      "iip-thumb.smk.dk",
    ],
  },
};

export default nextConfig;
